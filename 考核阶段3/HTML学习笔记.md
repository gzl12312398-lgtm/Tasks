# HTML学习笔记

**1.常见文本标签**

**2.HTML属性**

<a>标签的属性

 1.herf 用于引用链接

如：<a href="https://github.com/gzl12312398-lgtm">这是第一个超链接</a>

 2.target 用于定义链接的打开方式 （鼠标放在target上即可查看不同属性值的介绍）

target=“_blank“    链接在新的窗口打开

target=“_self“        链接在当前窗口或者标签页中打开

target=“_parent“  链接在父窗口或者父框架中打开

target=“_top“        链接在顶层窗口或者顶层框架中打开



<img>图片标签的属性

<img src="" alt="">

1.src 定义要显示文件的路径

2.alt 定义图像的替代文本

3.width 设置图像的宽 height 设置图像的高



<input type="text"> （W3Cschool网站）

1.type 属性规定了input元素的类型

2.placehoder 填一些想在文本框中显示的内容，在输入一些内容后会消失

3.value  填一些想在文本框中显示的内容，用鼠标点一下文本框此内容就输入进去了

4.submit  把填写的内容提交



**3.HTML区块** 

(1) 块元素(block)  通常用于组织和布局页面主要结构和内容，如段落，标题，列表，表格等。它们用于创建页面的主要部分，将内容分隔成逻辑块。

- 块级元素通常会从新行开始，并占据整行的宽度，因此它们会在页面上呈现为一块独立的内容块。

- 可以包含其他块级元素和行内元素。

- 常见的块级元素包括  ‘<div>’ ,‘<p>’,‘<h1>’到‘<h6>’,‘<ul>’,‘<ol>’,‘<li>’,‘<table>’,‘<form>’ 等。

  (2) 行内元素(inline)

- 行内元素通常在同一行内呈现，不会独占一行。

- 它们只占据其内容所需的宽度，而不是整行的宽度。

- 行内元素不能包含块级元素，但可以包含其他行内元素。

- 常见的行内元素包括‘<span>’,‘<a>’,‘<strong>’,‘<em>’,‘<img>’,‘<br>’,‘<input>’等。



